import GameTimer from './timer.js';
import { Player } from './player.js';

/* const gameTimer = new GameTimer(0.25);
function someFunction() {
  console.log('rampampam');
}

function intervalFunction(timeLeft, totalTime) {
  console.log(`Time: ${timeLeft} / ${totalTime}`);
}

gameTimer.runTimer(intervalFunction, someFunction); */

const player = new Player();
